[package]
name = "renormalization-group"
version = "0.1.0"
edition = "2024"
description = "Renormalization group framework for multi-scale fleet analysis"
license = "MIT"
repository = "https://github.com/SuperInstance/renormalization-group"
keywords = ["physics", "renormalization", "fleet", "multi-scale", "statistical-mechanics"]
categories = ["science", "mathematics"]

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
